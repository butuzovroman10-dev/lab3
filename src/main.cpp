#include "matrix.h"

int main() {
    try{
        std::string input;
        std::string result;
        std::cout << "Введите строки (для завершения введите 'end'):" << std::endl;
        char a=0;
        while (true){
            std::getline(std::cin, input);
            
        }

    }
    catch(...){

    }
}